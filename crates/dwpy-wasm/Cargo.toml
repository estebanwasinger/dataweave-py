[package]
name = "dwpy-wasm"
version = "0.1.0"
description = "WASM bridge foundation for the DataWeave Rust runtime"
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
dwpy-core = { path = "../dwpy-core" }
serde_json = "1"
wasm-bindgen = "0.2"
