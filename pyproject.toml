[project]
name = "dataweave-py"
version = "0.1.0"
description = "DataWeave interpreter running natively on Python"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [

]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "ipykernel>=7.1.0",
]
