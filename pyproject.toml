[project]
name = "dataweave-py"
version = "0.1.0"
description = "DataWeave interpreter running natively on Python"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.4.2",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
